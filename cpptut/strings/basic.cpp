#include<iostream>
#include<string>
using namespace std;
int main(){
    // string st;
    // cin>>st; // works only if string has no spaces 
    string s;
    getline(cin,s);
    cout<<s;
    // cout<<st;
}